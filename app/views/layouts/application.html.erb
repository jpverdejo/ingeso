<!DOCTYPE html>
<html>
  <head>
   <title>Control de Asistencia e Identificación - USACH</title>
    <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>

    <div class="navbar navbar-default navbar-static-top" role="navigation">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand">CAI USACH</a>
        </div>
        <div class="navbar-collapse collapse">
          <ul class="nav navbar-nav">
            <% if user_signed_in? %>
              <li><a href="/pruebas/revisar">Revisar Alumnos</a></li>
              <% if current_user.admin? %>
                <li class="dropdown">
                  <a href="/alumnos" class="dropdown-toggle" data-toggle="dropdown">Alumnos <b class="caret"></b></a>
                  <ul class="dropdown-menu">
                    <li><a href="/alumnos/">Lista de alumnos</a></li>
                    <li><a href="/alumnos/agregar">Agregar alumno</a></li>
                    <li><a href="/alumnos/cargar">Cargar alumnos</a></li>
                    <li><a href="/alumnos/cargarFotos">Cargar fotos</a></li>
                  </ul>
                </li>
                <li><a href="/pruebas">Pruebas</a></li>
                <li><a href="/users">Usuarios</a></li>
              <% end %>
            <% end %>
          </ul>
          <div class="navbar-right">
            <% if user_signed_in? %>
              <ul class="nav navbar-nav">
                <li><%= link_to "Salir", destroy_user_session_path, :method => :delete %></li>
              </ul>
            <% end %>
          </div>

        </div><!--/.nav-collapse -->
      </div>
    </div>

    <div class="container">

      <% if flash[:error] %>
        <div class="alert alert-warning"><%= flash[:error] %></div>
      <% end %>
      <% if flash[:notice] %>
        <div class="alert alert-info"><%= flash[:notice] %></div>
      <% end %>

      <%= yield %>
    </div><!-- /.container -->
  </body>
</html>
